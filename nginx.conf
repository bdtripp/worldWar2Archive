server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location /images/ {
        try_files $uri =404;
    }

    location /data/ {
        try_files $uri =404;
    }

    location /assets/ {
        try_files $uri =404;
    }
}